# Extension Environment Variables
#
# NOTE: The extension automatically reads from apps/web/.env.local
# You don't need to create a separate .env.local for the extension.
#
# The Vite config maps these variables:
#   NEXT_PUBLIC_SUPABASE_URL -> VITE_SUPABASE_URL
#   NEXT_PUBLIC_SUPABASE_ANON_KEY -> VITE_SUPABASE_ANON_KEY
#   NEXT_PUBLIC_APP_URL -> VITE_APP_URL
#
# Make sure apps/web/.env.local exists with your Supabase credentials.
# Get these from your Supabase project dashboard: https://supabase.com/dashboard
